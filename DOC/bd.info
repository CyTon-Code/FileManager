Стандарт как должна работать bd.

Первым делом нужно подключиться к базеданных
usr = user("root") = должен вернуть обьект который хранит:
адрес на базу данных...

Дальше внутри обьекта будут методы, первым аргументом указывается имя переменной, вторым что в нее положить или т.д. если первый аргумент этого требует.
usr.get("")
usr.get("arg_let")
агументы стандратно разделяються пробелами. 


#  s = "I like apple."
#  lst = s.split()

#  print(lst) # ['I', 'like', 'apple.']
